# Course notebooks

Jupyter notebooks will be added here as they are developed. The intended file
names are already recorded in the weekly cards in `public/index.html`.

After adding a notebook, set the matching card's `data-released` value to
`true`. With the repository owner configured in `public/site-config.js`, the
card will then open the notebook directly in Google Colab.
