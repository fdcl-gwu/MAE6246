# Homework files

Place student-facing homework statements in this directory. The homepage
currently provides placeholders named `homework01.pdf` through
`homework14.pdf`. These names may be changed in the matching `data-file`
attribute in `public/index.html`.

After adding a file, change its matching `data-released` value in
`public/index.html` from `false` to `true`.
